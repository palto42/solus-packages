# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : gnome-terminal
version    : 3.60.0
release    : 69
source     :
    - https://download.gnome.org/sources/gnome-terminal/3.60/gnome-terminal-3.60.0.tar.xz : b8daf3f08545172c4d2885733f720361ab0349ea669b99245eed4ad16ed3de28
homepage   : https://gitlab.gnome.org/GNOME/gnome-terminal
license    : GPL-3.0-or-later
component  : desktop.gnome
summary    : GNOME Terminal Emulator
description: |
    GNOME Terminal Emulator
builddeps  :
    - pkgconfig(gsettings-desktop-schemas)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(libhandy-1)
    - pkgconfig(libnautilus-extension-4)
    - pkgconfig(libpcre2-8)
    - pkgconfig(vte-2.91)
    - pkgconfig(yelp-xsl)
    - itstool
    - yelp-tools
setup      : |
    %patch -p1 -i $pkgfiles/0001-Enforce-Solus-Defaults.patch

    %meson_configure -Dsearch_provider=false
build      : |
    %ninja_build
install    : |
    %ninja_install
    # NOTE: try removing this for gnome 48 and see what breaks
    ln -sv gnome-terminal $installdir/usr/bin/x-terminal-emulator
