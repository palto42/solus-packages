name       : gnupg
version    : 2.4.8
release    : 47
source     :
    - https://gnupg.org/ftp/gcrypt/gnupg/gnupg-2.4.8.tar.bz2 : b58c80d79b04d3243ff49c1c3fc6b5f83138eb3784689563bcdd060595318616
license    : GPL-3.0-or-later
homepage   : https://www.gnupg.org
summary    : Complete and free implementation of OpenPGP
component  : security
description: |
    GnuPG is a complete and free implementation of OpenPGP standard as defined by RFC4880. It allows encryption and signing of data and communications, a key management system, and access modules for public key directories.
builddeps  :
    - pkgconfig(bzip2)
    - pkgconfig(gnutls)
    - pkgconfig(gpg-error)
    - pkgconfig(ksba)
    - pkgconfig(libassuan)
    - pkgconfig(libgcrypt)
    - pkgconfig(sqlite3)
    - npth-devel
    - pinentry
rundeps    :
    - pinentry
setup      : |
    %configure \
               --enable-symcryptrun \
               --enable-gpgtar \
               --enable-all-tests
build      : |
    %make
install    : |
    %make_install DESTDIR=$installdir
    ln -s /usr/bin/gpg $installdir/usr/bin/gpg2
check      : |
    # If tests last too long, you can remove --enable-all-tests
    %make check
