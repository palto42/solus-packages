From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Silke Hofstra <silke@slxh.eu>
Date: Wed, 9 Aug 2023 16:42:41 +0200
Subject: [PATCH 2/4] Hardcode Solus UEFI ID

---
 plugins/uefi-capsule/fu-uefi-capsule-device.c | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/plugins/uefi-capsule/fu-uefi-capsule-device.c b/plugins/uefi-capsule/fu-uefi-capsule-device.c
index 2745c0d08..9e7b8a802 100644
--- a/plugins/uefi-capsule/fu-uefi-capsule-device.c
+++ b/plugins/uefi-capsule/fu-uefi-capsule-device.c
@@ -737,6 +737,7 @@ fu_uefi_capsule_device_build_efi_path(FuUefiCapsuleDevice *self,
 	g_autofree gchar *os_release_id = NULL;
 	g_autofree gchar *os_release_id_like = NULL;
 	g_autofree gchar *systemd_path = NULL;
+	g_autofree gchar *solus_path = NULL;
 #endif
 
 	/* sanity check */
@@ -753,6 +754,11 @@ fu_uefi_capsule_device_build_efi_path(FuUefiCapsuleDevice *self,
 	va_end(args);
 
 #ifndef EFI_OS_DIR
+	/* Solus default ID */
+	solus_path = g_build_filename(esp_path, "EFI", "com.solus-project", NULL);
+	if (g_file_test(solus_path, G_FILE_TEST_IS_DIR))
+		return g_build_filename(solus_path, filename_built, NULL);
+
 	/* distro (or user) is using systemd-boot */
 	systemd_path = g_build_filename(esp_path, "EFI", "systemd", NULL);
 	if (g_file_test(systemd_path, G_FILE_TEST_IS_DIR))
